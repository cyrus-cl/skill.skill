# Python Code Review Workflow

> 从代码审查对话中抽取稳定的审查顺序、风险优先级、测试关注点与提交信息规范，生成可复用的代码审查 skill 草稿。

## 适用场景
- 需要多轮校准 Python 代码审查口径、严重级别和输出格式
- 希望固定风险优先、测试导向的 review 工作流
- 需要把提交信息规范与审查重点一起沉淀为长期规则

## 不适用场景
- 只做一次性的代码解释或教学说明
- 没有代码或 diff 的泛泛审查请求
- 只想修饰表达，不涉及真实风险判断
- 以 UI 文案润色为主的非工程审查任务
- 仍在频繁切换审查重点、顺序和输出格式的探索阶段

## 输入要求
- 当前任务的 transcript 或本次对话摘要
- 与任务相关的输入材料或目标输出对象
- 如需合并，额外提供已有 skill Markdown 文件

## 通用层规则（所有同类任务通用）
- 不要先说命名风格，先找行为风险和回归风险
- 结论按严重级别排序，先 blocking 再 non-blocking
- 每条意见都要写影响和建议
- 提交信息也一起给建议，统一用 Conventional Commits，scope 必写
- 现在这套审查口径稳定了，以后类似 Python PR 都按这个来

## 个人层规则（你的专属工作习惯）
- 文风偏好：不要写成长篇教学文章，短句、直接、可执行
- 结构偏好：未明确提取
- 禁用表达：我做 review 时会特别看测试缺口、异常处理和数据库事务边界
- 严谨程度：未明确提取
- 其他个人习惯：未明确提取

## 长期规则
- 不要先说命名风格，先找行为风险和回归风险
- 结论按严重级别排序，先 blocking 再 non-blocking
- 每条意见都要写影响和建议
- 提交信息也一起给建议，统一用 Conventional Commits，scope 必写
- 现在这套审查口径稳定了，以后类似 Python PR 都按这个来
- 我做 review 时会特别看测试缺口、异常处理和数据库事务边界
- 不要写成长篇教学文章，短句、直接、可执行

## 当次特例
- 这次先忽略 UI 文案，因为这个 PR 只改后端
- 这是本次特例，以后默认还是正常检查范围

## 必做步骤
1. 先看行为风险和回归风险，再看风格问题。
2. 按严重级别排序审查结论，并给出影响与修复建议。
3. 重点检查测试缺口、异常处理和事务边界。
4. 补充符合规范的提交信息建议。

## 输出规范
- 不要先说命名风格，先找行为风险和回归风险
- 结论按严重级别排序，先 blocking 再 non-blocking
- 每条意见都要写影响和建议
- 我做 review 时会特别看测试缺口、异常处理和数据库事务边界
- 不要写成长篇教学文章，短句、直接、可执行
- 现在这套审查口径稳定了，以后类似 Python PR 都按这个来

## 负面约束
- 不要先说命名风格，先找行为风险和回归风险
- 结论按严重级别排序，先 blocking 再 non-blocking
- 我做 review 时会特别看测试缺口、异常处理和数据库事务边界
- 不要写成长篇教学文章，短句、直接、可执行
- 这次先忽略 UI 文案，因为这个 PR 只改后端

## 示例
输入示例：
- 提供一份包含多轮纠偏的 Python Code Review Workflow transcript
- 当前任务属于：需要多轮校准 Python 代码审查口径、严重级别和输出格式

输出示例：
- 先给出通用层规则与个人层规则的分离结果
- 再给出执行步骤、输出规范、负面约束与规则来源
- 明确标注哪些内容属于当次特例，不能直接固化

## 维护记录
- 版本：v0.1
- 创建时间：2026-04-13
- 来源对话：examples/code-review-transcript.md
- 下次更新建议：当连续两次以上新增同类纠偏时，重新运行蒸馏并评估是否升级版本。

## 后续维护建议
- 先用确认模板删除误提取规则，再决定是否创建最终 skill。
- 如果未来出现相似旧 skill，优先运行 merge_candidate.py 做增量合并。
- 如果连续 3 次使用仍需新增同类规则，说明当前版本仍需继续迭代。


## 规则来源
- 规则：不要先说命名风格，先找行为风险和回归风险
  来源：对话第2轮：用户说“不要先说命名风格，先找行为风险和回归风险”
- 规则：结论按严重级别排序，先 blocking 再 non-blocking
  来源：对话第3轮：用户说“结论按严重级别排序，先 blocking 再 non-blocking”
- 规则：每条意见都要写影响和建议
  来源：对话第3轮：用户说“每条意见都要写影响和建议”
- 规则：提交信息也一起给建议，统一用 Conventional Commits，scope 必写
  来源：对话第5轮：用户说“提交信息也一起给建议，统一用 Conventional Commits，scope 必写”
- 规则：现在这套审查口径稳定了，以后类似 Python PR 都按这个来
  来源：对话第8轮：用户说“现在这套审查口径稳定了，以后类似 Python PR 都按这个来”
- 规则：我做 review 时会特别看测试缺口、异常处理和数据库事务边界
  来源：对话第4轮：用户说“我做 review 时会特别看测试缺口、异常处理和数据库事务边界”
- 规则：不要写成长篇教学文章，短句、直接、可执行
  来源：对话第6轮：用户说“不要写成长篇教学文章，短句、直接、可执行”
- 规则：这次先忽略 UI 文案，因为这个 PR 只改后端
  来源：对话第7轮：用户说“这次先忽略 UI 文案，因为这个 PR 只改后端”
- 规则：这是本次特例，以后默认还是正常检查范围
  来源：对话第7轮：用户说“这是本次特例，以后默认还是正常检查范围”
